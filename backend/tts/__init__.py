"""Модуль озвучивания текста (Text-to-Speech).

Зеркало backend.asr: там «аудио → текст», здесь «текст → аудио».
Синтез — Silero (офлайн, CPU). Экспорт — через PyAV (без внешнего ffmpeg),
как и декодирование в backend.asr.base.

Импорт пакета намеренно лёгкий: torch/av подтягиваются только при первом
обращении к engine/export, а не при `import backend.tts`.
"""
