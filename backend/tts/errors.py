"""Ошибки модуля TTS."""

from __future__ import annotations


class CloudTtsError(RuntimeError):
    """Персистентный сбой облачного TTS (неверный ключ, нет сети после ретраев,
    не удалось декодировать ответ).

    В отличие от обычного сбоя синтеза одного фрагмента (когда pipeline подставляет
    тишину), эту ошибку pipeline НЕ глушит — она прерывает генерацию книги с понятным
    сообщением, иначе при сбое облака вся книга ушла бы в молчание.
    """
