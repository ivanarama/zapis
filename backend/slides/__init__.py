"""Презентация + сценарий → видео с озвучкой.

Зачем. Доклад со слайдами не всегда можно показать вживую: площадки нередко
принимают только видео или картинки. Пересобрать презентацию в видео вручную
трудно из-за анимации — показ состоит не из слайдов, а из их состояний, которые
докладчик открывает нажатием Enter.

Порядок работы:

    frames.render(...)   презентация → кадры (анимация развёрнута по состояниям)
    script.parse/bind    сценарий с пометками [6], [6$] → куски текста на кадры
    pipeline.narrate     синтез через backend.tts, длительность кадра = длине фразы
    video.build          кадры + дорожка → mp4

Синтез, ударения и нормализация целиком переиспользуют backend.tts — здесь
только то, чего в книжном пути нет: разбор анимации, привязка текста к кадрам и
сборка видео.
"""

from __future__ import annotations

from . import frames, pipeline, script, video

__all__ = ["frames", "script", "pipeline", "video"]
