# Pinned to the known-good set installed in venv311 (Python 3.11) so the
# GitHub Actions build doesn't drift. Bump deliberately, not implicitly.

fastapi==0.136.1
uvicorn==0.46.0
httpx==0.28.1
python-multipart==0.0.27
pydantic==2.13.4
pywebview==6.2.1
pyinstaller==6.20.0

# ASR — GigaAM v3 (Source of truth: C:/Projects/gigaam_tone)
# ВАЖНО: PyPI-версия gigaam поддерживает только v1/v2. Для v3 ставить с GitHub.
# Пин на конкретный коммит — апстрим иногда ломает API (см. _MODEL_HASHES/_MODEL_NAMES).
# build.ps1 делает force-reinstall этого же коммита, чтобы перебить возможную PyPI-версию.
gigaam @ git+https://github.com/salute-developers/GigaAM.git@6e4b027c6fb554e09e8b9059b757a175295ab879
# pyctcdecode 0.5.0 жёстко требует numpy<2.0.0, а gigaam — numpy==2.*; вместе не
# резолвятся. На деле pyctcdecode прекрасно работает с numpy 2.x — поэтому ставим
# его отдельно в build.ps1 c --no-deps (его единственная реальная зависимость —
# pygtrie ниже).
torch==2.5.1
# gigaam imports torchaudio (preprocess.py: MelSpectrogram) but doesn't declare it
torchaudio==2.5.1
numpy==2.4.4
scipy==1.17.1
sentencepiece==0.2.1
pygtrie==2.5.0
huggingface-hub==1.14.0
# huggingface-hub 1.x качает через httpx, а тот доверяет только certifi и
# игнорирует хранилище сертификатов ОС. На машинах с корпоративным TLS-прокси
# из-за этого не скачиваются модели, хотя в браузере HF открывается.
# truststore перенаправляет проверку в системное хранилище (см. main.py).
truststore==0.10.4

# ASR — faster-whisper для не-русских языков
faster-whisper==1.2.1

# Диаризация («кто говорит») — sherpa-onnx. Модели те же, что у
# pyannote/speaker-diarization-3.1 (segmentation-3.0 + WeSpeaker ResNet34-LM),
# но в ONNX и с GitHub Releases: оригиналы на HF закрыты гейтом и на машине
# пользователя не скачались бы. torch не нужен — внутри своя копия
# onnxruntime, поэтому ставится одним колесом плюс sherpa-onnx-core (~19 МБ).
# Веса (~34 МБ) качаются при первом включении, в бинарь не пакуются.
sherpa-onnx==1.13.7

# LLM — OpenAI-совместимый клиент с асинхронным стримингом
openai==2.36.0
# anthropic — опционально, нужен только при выборе провайдера "anthropic" в профиле
anthropic==0.100.0

# TTS — озвучивание текста (Silero грузится через torch.hub, отдельного пакета не требует).
# Экспорт аудио — через PyAV (av), который уже приходит транзитивно с faster-whisper.
razdel==0.5.0      # сегментация русских предложений для нарезки
num2words==0.5.13  # числа → слова (rule-based нормализация без LLM)
# Ударения для синтеза: ruaccent ставит «+» над ударными гласными (формат Silero).
# Тянет transformers/tokenizers/safetensors — веса омограф-модели качаются с HF
# при первом запуске (как Silero/GigaAM). tokenizers подтягивается под transformers
# 5.x и совместим с faster-whisper (>=0.13,<1). Пины — под known-good venv311.
ruaccent==1.5.8.3
transformers==5.12.1
tokenizers==0.22.2
safetensors==0.8.0
# Движок «качество» — Piper (MIT). Нативный wheel под Windows со встроенным
# espeak-ng; голоса ru_RU качаются с HF при первом использовании. onnxruntime
# уже стоит (gigaam). pathvalidate — единственная новая зависимость piper.
piper-tts==1.4.2
pathvalidate==3.3.1
# Облачный TTS «без ключа» — Microsoft Edge neural voices (edge-tts). Без API-ключа
# и регистрации (публичный endpoint «Читать вслух»). Тянет aiohttp. endpoint
# неофициальный (реверс-инжиниринг) — только личное использование.
edge-tts==7.2.8
aiohttp==3.14.3
